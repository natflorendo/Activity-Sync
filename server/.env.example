# .env.example
# Rename this file to `.env` and replace all placeholder values with your actual credentials.
# Do NOT commit `.env`.

# --- Node Environment ---
# Set to "development", "production", or "test"
NODE_ENV="development"

# --- Database Configuration ---
# Format: postgresql://username:password@host:port/database
DATABASE_URL="postgresql://<DB_USER>:<DB_PASSWORD>@localhost:5432/<DB_NAME>"

# --- CORS ---
# Comma-separated list of origins allowed to call your API (no spaces).
ALLOWED_ORIGINS=http://localhost:5173,https://your-frontend-domain.com

# --- Authentication ---
# Used by Starlette's SessionMiddleware to SIGN (not encrypt) the session cookie.
# Required for Authlib OAuth flows (stores OAuth state/nonce)
SESSION_SECRET="change_me_session_secret"
# Secret used to sign JWT tokens (keep this safe in production)
JWT_SECRET="your_jwt_secret_here"

# --- Google OAuth Credentials ---
# These are required for Google login & Calendar integration
GOOGLE_CLIENT_ID="your_google_client_id_here"
GOOGLE_CLIENT_SECRET="your_google_client_secret_here"

# --- Strava OAuth Credentials ---
# These are required for Strava login integration & activity sync
STRAVA_CLIENT_ID="your_strava_client_id"
STRAVA_CLIENT_SECRET="your_strava_client_secret"
# Used to verify Strava's initial webhook challenge
STRAVA_VERIFY_TOKEN="random_string_you_choose"

# --- Frontend URL ---
# Used to redirect OAuth back to the frontend
FRONTEND_URL="http://localhost:5173"

# --- Backend URL ---
# Used to construct the Google and Strava OAuth callback URL
BACKEND_URL="http://localhost:8000"

# --- Backend Domain ---
# Used for setting cookies in production (e.g., activitysync-api.onrender.com)
BACKEND_DOMAIN="localhost"